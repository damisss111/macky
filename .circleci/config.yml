version: 2.1

jobs:
  crd-debug:
    machine: true
    steps:
      - run:
          name: Keep Alive Forever (SSH & Python Ready)
          command: |
            echo "🟢 VM is running. SSH in from CircleCI → 'Rerun with SSH'"
            echo "⏳ This VM will stay alive for 5 hours unless cancelled."
            echo "🐍 You can install CRD manually using Python or Bash."
            sudo apt update
            sudo apt install -y python3 curl wget sudo lsb-release
            echo "Ready for your Python code..."
            while true; do sleep 60; done

workflows:
  version: 2.1
  crd-workflow:
    jobs:
      - crd-debug